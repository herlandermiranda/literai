{
  "buildCommand": "pnpm install && pnpm build",
  "outputDirectory": "dist",
  "env": {
    "VITE_API_BASE_URL": "http://localhost:8000"
  },
  "rewrites": [
    {
      "source": "/api/v1/:path*",
      "destination": "https://literai-backend.up.railway.app/api/v1/:path*"
    }
  ]
}